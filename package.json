{
  "name": "codex-prep",
  "version": "0.1.0",
  "description": "Local-first Codex onboarding and codebase intelligence kit.",
  "type": "module",
  "bin": {
    "codex-prep": "./bin/codex-prep.js"
  },
  "scripts": {
    "lint": "node ./bin/codex-prep.js lint --repo .",
    "test": "node --test",
    "verify": "npm run test && npm run lint && node ./bin/codex-prep.js check --repo . && node ./bin/codex-prep.js eval --repo .",
    "start": "node ./bin/codex-prep.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
